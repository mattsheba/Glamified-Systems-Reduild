---
import site, { whatsappHref } from "../lib/site";

/**
 * Persistent WhatsApp button, fixed to the bottom corner of every page.
 *
 * It replaces the WhatsApp button that used to sit in the footer. A footer
 * button only works for someone who has already scrolled to the bottom; this is
 * reachable from any point on the page, which matters most on the long product
 * and service pages.
 *
 * The brand green is used deliberately rather than the site accent: people
 * recognise the WhatsApp mark by its colour, and recolouring it to fit the
 * palette makes it read as a generic chat bubble.
 */
const wa = whatsappHref();
const label = site.hero.primaryCta.label;
---

<a class="wa-float" href={wa} rel="noopener" aria-label={label}>
  {/* The official glyph. Drawn inline rather than loaded, because one 700-byte
      path costs less than a request and it has to work offline. */}
  <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
    <path
      fill="currentColor"
      d="M17.47 14.38c-.3-.15-1.75-.86-2.02-.96-.27-.1-.47-.15-.67.15-.2.3-.77.96-.94 1.16-.17.2-.35.22-.64.08-.3-.15-1.25-.46-2.38-1.47-.88-.78-1.47-1.75-1.65-2.05-.17-.3-.02-.46.13-.6.13-.13.3-.35.45-.52.15-.17.2-.3.3-.5.1-.2.05-.37-.02-.52-.08-.15-.67-1.6-.92-2.2-.24-.58-.49-.5-.67-.5-.17-.01-.37-.01-.57-.01-.2 0-.52.07-.79.37-.27.3-1.04 1.01-1.04 2.47s1.06 2.87 1.21 3.07c.15.2 2.1 3.2 5.08 4.49.71.3 1.26.49 1.69.63.71.22 1.36.19 1.87.12.57-.09 1.75-.72 2-1.41.25-.69.25-1.28.17-1.41-.07-.13-.27-.2-.57-.35z"
    />
    <path
      fill="currentColor"
      d="M12.04 2C6.58 2 2.13 6.45 2.13 11.91c0 1.75.46 3.45 1.32 4.95L2 22l5.25-1.38a9.86 9.86 0 0 0 4.79 1.22h.01c5.46 0 9.91-4.45 9.91-9.91 0-2.65-1.03-5.14-2.9-7.01A9.82 9.82 0 0 0 12.04 2zm0 18.13h-.01a8.23 8.23 0 0 1-4.19-1.15l-.3-.18-3.12.82.83-3.04-.2-.31a8.19 8.19 0 0 1-1.26-4.36c0-4.54 3.7-8.24 8.25-8.24 2.2 0 4.27.86 5.83 2.42a8.19 8.19 0 0 1 2.41 5.83c0 4.54-3.7 8.24-8.24 8.24z"
    />
  </svg>
</a>

<style>
  .wa-float {
    position: fixed;
    right: clamp(16px, 3vw, 28px);
    bottom: clamp(16px, 3vw, 28px);
    /* Above page content, below the sticky header (z-index 50) so a dropdown
       opening over it still wins. */
    z-index: 45;
    display: grid;
    place-items: center;
    width: 56px;
    height: 56px;
    border-radius: 50%;
    /* WhatsApp brand green, not --accent. See the note above. */
    background: #25d366;
    color: #ffffff;
    box-shadow: rgba(0, 0, 0, 0.28) 0 4px 14px;
    transition: transform 0.18s ease, box-shadow 0.18s ease;
  }

  .wa-float svg {
    width: 32px;
    height: 32px;
  }

  .wa-float:hover,
  .wa-float:focus-visible {
    transform: translateY(-2px);
    box-shadow: rgba(0, 0, 0, 0.34) 0 6px 18px;
  }

  /* 56px already clears the 44px minimum, so no coarse-pointer padding here. */
  @media (max-width: 400px) {
    .wa-float {
      width: 52px;
      height: 52px;
    }
    .wa-float svg {
      width: 29px;
      height: 29px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .wa-float {
      transition: none;
    }
    .wa-float:hover,
    .wa-float:focus-visible {
      transform: none;
    }
  }
</style>
